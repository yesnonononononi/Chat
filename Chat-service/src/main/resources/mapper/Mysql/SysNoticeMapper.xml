<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.summit.chat.Mapper.Mysql.SysNoticeMapper">

    <insert id="insert" parameterType="com.summit.chat.model.entity.mysql.SysNotice" useGeneratedKeys="true" keyProperty="id">
        insert into sys_notice (
            msg, create_time, end_time, publisher_id, version, is_deleted
        ) values (
            #{msg}, #{createTime}, #{endTime}, #{publisherId}, #{version}, #{isDeleted}
        )
    </insert>

    <update id="update" parameterType="com.summit.chat.model.entity.mysql.SysNotice">
        update sys_notice
        <trim prefix="SET" suffixOverrides=",">
            <if test="msg != null and msg != ''">msg = #{msg},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="publisherId != null">publisher_id = #{publisherId},</if>
            <if test="version != null">version = #{version},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted},</if>
        </trim>
        where id = #{id}
    </update>

    <select id="queryLikeList" resultType="com.summit.chat.model.vo.NoticeLikeVO">
        select sn.id as noticeId,sn.`like` from sys_notice sn
        <where>
        sn.id in
            <foreach collection="list" item="item" open="(" close=")" separator=",">
                #{item}
            </foreach>
        </where>
    </select>

</mapper>
